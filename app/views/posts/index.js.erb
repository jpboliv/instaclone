$('#posts').append("<%= escape_javascript(render @posts)%>");
$('#paginator-button').html("<%= escape_javascript(render( "button_more", posts: @posts)) %>");
<% if @posts.last_page? %> { 
    $('#paginator-button').remove(); 
}
<% end %>
